<!-- STUB — Phase 2 agent A7 replaces this with frame XJ6t0. -->
<script lang="ts">
	let { id }: { id: number } = $props();
</script>

<div class="stub">
	<h2>GameProfile #{id}</h2>
	<p>Placeholder — implement bg-picker.pen frame XJ6t0.</p>
</div>

<style>
	.stub {
		padding: 40px;
		margin: 40px auto;
		max-width: 800px;
		background: var(--surface-secondary);
		border: 2px dashed var(--border-subtle);
		border-radius: var(--radius-lg);
		text-align: center;
		color: var(--foreground-secondary);
	}
</style>
